<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.BorderPane?>
<?import javafx.scene.layout.VBox?>
<?import javafx.scene.layout.StackPane?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.layout.GridPane?>
<?import javafx.scene.layout.ColumnConstraints?>
<?import javafx.scene.control.TextField?>
<?import javafx.scene.control.PasswordField?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Hyperlink?>

<?import java.net.URL?>
<BorderPane fx:controller="com.example.dacs4.controllers.LoginController"
            xmlns="http://javafx.com/javafx/17"
            xmlns:fx="http://javafx.com/fxml/1"
            prefWidth="1200" prefHeight="800"
            styleClass="login-root">
    <!-- CSS sẽ được load bằng code Java sau khi load FXML -->


    <center>
        <HBox spacing="40" alignment="CENTER" styleClass="login-container">

            <!-- LEFT: BRANDING -->
            <VBox spacing="20" alignment="CENTER_LEFT"
                  HBox.hgrow="ALWAYS"
                  styleClass="branding-box">
                <HBox spacing="12" alignment="CENTER_LEFT">
                    <StackPane prefWidth="64" prefHeight="64"
                               styleClass="logo-box">
                        <Label text="▶" styleClass="logo-icon"/>
                    </StackPane>
                    <Label text="MeetHub" styleClass="app-name"/>
                </HBox>

                <VBox spacing="8">
                    <Label text="Nền tảng họp trực tuyến chuyên nghiệp"
                           styleClass="headline"/>
                    <Label text="Kết nối với đồng nghiệp và khách hàng của bạn một cách dễ dàng và bảo mật"
                           wrapText="true"
                           styleClass="subheadline"/>
                </VBox>

                <GridPane hgap="16" vgap="16">
                    <columnConstraints>
                        <ColumnConstraints percentWidth="50"/>
                        <ColumnConstraints percentWidth="50"/>
                    </columnConstraints>

                    <!-- Bảo mật -->
                    <VBox GridPane.rowIndex="0" GridPane.columnIndex="0"
                          spacing="4" styleClass="feature-card">
                        <Label text="🔒" styleClass="feature-icon"/>
                        <Label text="Bảo mật" styleClass="feature-title"/>
                        <Label text="Mã hóa end-to-end" styleClass="feature-text"/>
                    </VBox>

                    <!-- Đa nền tảng -->
                    <VBox GridPane.rowIndex="0" GridPane.columnIndex="1"
                          spacing="4" styleClass="feature-card">
                        <Label text="👥" styleClass="feature-icon"/>
                        <Label text="Đa nền tảng" styleClass="feature-title"/>
                        <Label text="Hỗ trợ mọi thiết bị" styleClass="feature-text"/>
                    </VBox>

                    <!-- HD Video -->
                    <VBox GridPane.rowIndex="1" GridPane.columnIndex="0"
                          spacing="4" styleClass="feature-card">
                        <Label text="🎥" styleClass="feature-icon"/>
                        <Label text="HD Video" styleClass="feature-title"/>
                        <Label text="Chất lượng cao" styleClass="feature-text"/>
                    </VBox>

                    <!-- Riêng tư -->
                    <VBox GridPane.rowIndex="1" GridPane.columnIndex="1"
                          spacing="4" styleClass="feature-card">
                        <Label text="🛡" styleClass="feature-icon"/>
                        <Label text="Riêng tư" styleClass="feature-title"/>
                        <Label text="Dữ liệu an toàn" styleClass="feature-text"/>
                    </VBox>
                </GridPane>
            </VBox>

            <!-- RIGHT: FORM CARD -->
            <VBox spacing="16" alignment="TOP_CENTER"
                  prefWidth="380"
                  styleClass="form-card">

                <VBox spacing="4" alignment="CENTER_LEFT">
                    <Label fx:id="titleLabel" styleClass="form-title"
                           text="Đăng nhập"/>
                    <Label fx:id="descriptionLabel" styleClass="form-description"
                           text="Đăng nhập để tham gia cuộc họp"/>
                </VBox>

                <VBox spacing="8">
                    <!-- Họ tên (chỉ hiện khi đăng ký) -->
                    <VBox spacing="4" fx:id="nameFieldContainer" visible="false" managed="false">
                        <Label text="Họ và tên"/>
                        <TextField fx:id="nameField"
                                   promptText="Nguyễn Văn A"/>
                    </VBox>

                    <!-- Email -->
                    <VBox spacing="4">
                        <Label text="Email"/>
                        <TextField fx:id="emailField"
                                   promptText="example@company.com"/>
                    </VBox>

                    <!-- Mật khẩu -->
                    <VBox spacing="4">
                        <Label text="Mật khẩu"/>
                        <PasswordField fx:id="passwordField"
                                       promptText="••••••••"/>
                    </VBox>

                    <Button fx:id="submitButton"
                            text="Đăng nhập"
                            maxWidth="Infinity"
                            onAction="#onSubmit"/>
                </VBox>

                <HBox alignment="CENTER">
                    <Hyperlink fx:id="toggleModeLink"
                               text="Chưa có tài khoản? Đăng ký"
                               onAction="#onToggleMode"/>
                </HBox>

            </VBox>
        </HBox>
    </center>

</BorderPane>
